{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "demo-p-otp-field-03",
  "title": "Otp Field 3",
  "description": "OTP field with separator",
  "registryDependencies": [
    "@usespaceui/primitives-otp-field"
  ],
  "files": [
    {
      "path": "src/registry/demo/primitives/otp-field/p-otp-field-03/index.tsx",
      "content": "import { OTPField, OTPFieldInput, OTPFieldSeparator } from '@/components/ui/otp-field'\n\nconst OTP_LENGTH = 6\nconst GROUP_LENGTH = 3\n\nconst OTP_SLOT_KEYS = Array.from({ length: OTP_LENGTH }, (_, i) => `otp-slot-${i}`)\n\nexport default function Particle() {\n  return (\n    <OTPField aria-label=\"Verification code\" length={OTP_LENGTH}>\n      {OTP_SLOT_KEYS.slice(0, GROUP_LENGTH).map((slotKey, index) => (\n        <OTPFieldInput key={slotKey} aria-label={index === 0 ? undefined : `Character ${index + 1} of ${OTP_LENGTH}`} />\n      ))}\n      <OTPFieldSeparator />\n      {OTP_SLOT_KEYS.slice(GROUP_LENGTH).map((slotKey, index) => (\n        <OTPFieldInput key={slotKey} aria-label={`Character ${index + GROUP_LENGTH + 1} of ${OTP_LENGTH}`} />\n      ))}\n    </OTPField>\n  )\n}\n",
      "type": "registry:ui",
      "target": "components/spaceui/otp-field.tsx"
    }
  ],
  "categories": [
    "otp field",
    "input"
  ],
  "type": "registry:ui"
}