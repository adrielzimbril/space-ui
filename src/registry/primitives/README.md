# 🌌 Space Primitives

Ce dossier `space` regroupe la **crème de la crème** des primitives, après une sélection impitoyable entre les différentes origines (majoritairement `base` vs `coss`).

Toutes les versions inférieures ou redondantes ont été définitivement supprimées du registre.

## ✅ Composants Conservés (Le top tier)

| Composant             | Origine Vainqueur | Justification                                     |
| --------------------- | ----------------- | ------------------------------------------------- |
| **accordion**         | `coss`            | Richesse des variantes et implémentation robuste. |
| **alert**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **alert-dialog**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **autocomplete**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **avatar**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **badge**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **breadcrumb**        | `coss`            | Richesse des variantes et implémentation robuste. |
| **calendar**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **card**              | `coss`            | Richesse des variantes et implémentation robuste. |
| **checkbox**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **checkbox-group**    | `coss`            | Richesse des variantes et implémentation robuste. |
| **collapsible**       | `coss`            | Richesse des variantes et implémentation robuste. |
| **combobox**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **command**           | `coss`            | Richesse des variantes et implémentation robuste. |
| **context-menu**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **dialog**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **drawer**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **empty**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **field**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **fieldset**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **files**             | `base`            | Unique composant disponible et suffisant.         |
| **form**              | `coss`            | Richesse des variantes et implémentation robuste. |
| **frame**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **group**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **input**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **input-group**       | `coss`            | Richesse des variantes et implémentation robuste. |
| **kbd**               | `coss`            | Richesse des variantes et implémentation robuste. |
| **label**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **menu**              | `coss`            | Richesse des variantes et implémentation robuste. |
| **meter**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **number-field**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **otp-field**         | `coss`            | Richesse des variantes et implémentation robuste. |
| **pagination**        | `coss`            | Richesse des variantes et implémentation robuste. |
| **popover**           | `coss`            | Richesse des variantes et implémentation robuste. |
| **preview-card**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **preview-link-card** | `base`            | Unique composant disponible et suffisant.         |
| **progress**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **radio**             | `base`            | Unique composant disponible et suffisant.         |
| **radio-group**       | `coss`            | Richesse des variantes et implémentation robuste. |
| **scroll-area**       | `coss`            | Richesse des variantes et implémentation robuste. |
| **select**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **separator**         | `coss`            | Richesse des variantes et implémentation robuste. |
| **sheet**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **sidebar**           | `coss`            | Richesse des variantes et implémentation robuste. |
| **skeleton**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **slider**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **spinner**           | `coss`            | Richesse des variantes et implémentation robuste. |
| **switch**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **table**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **tabs**              | `coss`            | Richesse des variantes et implémentation robuste. |
| **textarea**          | `coss`            | Richesse des variantes et implémentation robuste. |
| **toast**             | `coss`            | Richesse des variantes et implémentation robuste. |
| **toggle**            | `coss`            | Richesse des variantes et implémentation robuste. |
| **toggle-group**      | `coss`            | Richesse des variantes et implémentation robuste. |
| **toolbar**           | `coss`            | Richesse des variantes et implémentation robuste. |
| **tooltip**           | `coss`            | Richesse des variantes et implémentation robuste. |

## ❌ Composants Rejetés et Supprimés (Les doublons perdants)

| Composant        | Origine Perdante | Raison de la suppression                        |
| ---------------- | ---------------- | ----------------------------------------------- |
| **accordion**    | `base`           | Écrasé par la supériorité de la version `coss`. |
| **alert-dialog** | `base`           | Écrasé par la supériorité de la version `coss`. |
| **checkbox**     | `base`           | Écrasé par la supériorité de la version `coss`. |
| **collapsible**  | `base`           | Écrasé par la supériorité de la version `coss`. |
| **dialog**       | `base`           | Écrasé par la supériorité de la version `coss`. |
| **menu**         | `base`           | Écrasé par la supériorité de la version `coss`. |
| **popover**      | `base`           | Écrasé par la supériorité de la version `coss`. |
| **preview-card** | `base`           | Écrasé par la supériorité de la version `coss`. |
| **progress**     | `base`           | Écrasé par la supériorité de la version `coss`. |
| **switch**       | `base`           | Écrasé par la supériorité de la version `coss`. |
| **tabs**         | `base`           | Écrasé par la supériorité de la version `coss`. |
| **toggle**       | `base`           | Écrasé par la supériorité de la version `coss`. |
| **toggle-group** | `base`           | Écrasé par la supériorité de la version `coss`. |
| **tooltip**      | `base`           | Écrasé par la supériorité de la version `coss`. |
